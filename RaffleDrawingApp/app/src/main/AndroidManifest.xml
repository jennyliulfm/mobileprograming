<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="raffle.drawing">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MarginDrawingDetail"></activity>
        <activity android:name=".MarignDrawing" />
        <activity
            android:name=".NormalDrawing"
            android:label="Drawing Raffle" />
        <activity
            android:name=".DeleteTicket"
            android:label="Delete Ticket" />
        <activity
            android:name=".EditTicketDetail"
            android:label="Edit Ticket Detail" />
        <activity
            android:name=".ViewTicket"
            android:label="View Ticket" />
        <activity
            android:name=".EditTicket"
            android:label="Eidt Ticket" />
        <activity android:name=".TicketDetail" />
        <activity
            android:name=".SellTicket"
            android:label="Sell Ticket" />
        <activity
            android:name=".EditRaffle"
            android:label="Edit Raffle" />
        <activity
            android:name=".NewRaffle"
            android:label="New Raffle" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>